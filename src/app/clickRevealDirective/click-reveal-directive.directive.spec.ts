import { ClickRevealDirectiveDirective } from './click-reveal-directive.directive';

describe('ClickRevealDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickRevealDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
